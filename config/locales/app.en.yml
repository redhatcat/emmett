# Even if you only support one language, you can use this file to customise the names that appear in the UI
# e.g.

en:
  activerecord:
    attributes:
      entry:
        name: Title
        body_text: Body
        tag_string: Tags
    attribute_help:
      entry:
        tag_string: "Separate tags with commas ','.  Spaces are allowed."

